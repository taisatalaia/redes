Q 2.1=  6140 ms. Não. Pela informação gerada em ping consta 100%.

Q 2.2=  A banda entre sta1 e sta2 é 17.4 Mbps e 17.5 Mbps.

Q 2.3=
O comando py sta1.params apresenta informações de forma bastante especificada.
O comando py sta1.params[‘position’] apresenta esta informação [1.0, 0.0, 0.0].
O comando py sta1.setPosition(‘10,20,0’) informa que  o índice de lista está fora da faixa.
O comando distance sta1 sta2 informa a distância que é 1.00 metros.


Q 3.1= O sta3. Porque ele não esta ligado ao AP1. Não.

Q 3.2 = O nível de sinal da sta1 é -56 dbm. Quanto mais negativo ou mais longe o router estiver da máquina, pior fica o sinal . A sta1, sta2 e sta3 estão ligadas ao AP1.

Q 3.3=  Usaria em casa para conectar apenas um net, e o outro modelo eu usaria em uma biblioteca para fazer as conexões em  todas as maquinas.

Q 4.1= O comprimento  da sta1 para a sta2 é  31,47 metros.   A distancia é 17.4 e 17.5 Mbps.

Q 4.2= (CCA) é um dos dois mecanismos de sensores da operadora na WLAN (ou WiFi). É definido nos padrões IEEE 802.11-2007 como parte da camada de Protocolo de convergência de camada física  (PMD) e camada física (PLCP). CCA envolve duas funções relacionadas - CCA-CS e CCA-ED. 

Q 5.1=  Sim, porque quando o Sta1 está unido , o Sta1 sai da união  do Ap1 e se junta no Ap2.

Q 5.2= Chama-se Handover.

Q 6.1= Addmesh liga direto com os nós juntando  um ponto a outro, Addhoc não permiti fazer a conexão de forma automatica sem o roteamento estatico.
 
 
Q 6.3=A Sta1  está fora do alcance do Sta2 por isso está fora de comunicação. Para pesquisar com drones.






